{
  "name": "@openknowledge-sh/web",
  "private": true,
  "version": "0.12.0",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:viewer": "node scripts/dev-viewer.mjs",
    "build": "pnpm build:viewer && vite build && node scripts/build.mjs",
    "build:viewer": "vite build --config vite.viewer.config.js && vite build --config vite.theme.config.js",
    "lint": "oxlint src scripts vite.config.js vite.viewer.config.js vite.theme.config.js --deny-warnings",
    "test": "pnpm lint && vitest run",
    "test:browser": "pnpm build:viewer && node --test scripts/browser.e2e.mjs",
    "start": "OPENKNOWLEDGE_WEB_ROOT=dist OPENKNOWLEDGE_WEB_EXPORT_WIKI=0 HOST=0.0.0.0 node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "mermaid": "11.16.1"
  },
  "devDependencies": {
    "oxlint": "1.79.0",
    "vite": "6.4.3",
    "vitest": "3.2.6"
  }
}

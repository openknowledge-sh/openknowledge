# Corpus schema extension v1

`frontmatter.schema.json` defines the optional `corpus_schema` and
`typed_links` authored YAML shapes. Deterministic CLI validation adds the
cross-document type, path, metadata, link target, and migration checks.

This extension is not RDF, OWL, SHACL, or an automatic migration engine.

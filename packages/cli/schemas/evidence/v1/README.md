# Immutable evidence receipt v1

`receipt.schema.json` defines the source-specific receipt written by
`openknowledge evidence pin`. It binds exact artifact bytes and SHA-256 to the
declaring document, source ID, capture time, and original resource.

The receipt and artifact are immutable local evidence. Search indexes, graphs,
and runtime projections remain rebuildable.

# Runtime v1 schemas

`generation.schema.json` is the closed public contract for immutable runtime
generations. The manifest binds one source commit and OKF spec to the complete,
sorted `public/`, `source/`, `search/`, and `mcp/` file inventory and its
SHA-256 digests.
